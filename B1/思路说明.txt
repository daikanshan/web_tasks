Message类记录每个id留言的内容（id 作者 创建时间 留言）
GloInfo类记录全局信息（当前最大id 当前要显示的id(Array) 所有作者所对应的留言id(Hash)）

index:
	从全局信息里读取当前要显示的id（Array），遍历数组从messages/下读取信息放进视图

add：
  用户新建一条记录时，会从全局文件读取当前id并自加作为此留言的id，然后在messages/下新建以id.txt命名的文件保存此留言内容，最后更新全局信息文件

delete：
	更新全局信息文件后删除messages/下的对应id的文件

